# Maxy's collection of skills

## Install
`npx skills@latest add hbicmaxy/skills`

💬 annotate – `/anotate` will let you add comments to an HTML prototype and save them so you can hand over prototypes with context for engineering.
